// Intentionally empty of intent/regex routing.
// generate_visual is a static hot tool when enabled; the model decides when to call it.
// Do not reintroduce phrase-matching routing here — it cannot cover natural language exhaustively.
